{
  "name": "amber-compare-export",
  "private": true,
  "description": "Browser-based Amber Electric usage comparison and export tool",
  "scripts": {
    "build:css": "tailwindcss -i ./css/input.css -o ./css/app.css --minify",
    "test": "node tests/unit-time.js && playwright test",
    "test:unit": "node tests/unit-time.js",
    "serve": "node scripts/static-server.js"
  },
  "devDependencies": {
    "@playwright/test": "^1.55.0",
    "tailwindcss": "^3.4.17"
  }
}
